import css from './Filter.module.css';

const Filter = () => {
  return (
    <div className={css.filterContainer}>
      <p style={{ color: 'red', textAlign: 'center' }}>Filter</p>
    </div>
  );
};

export default Filter;
